# Skills

Each subdirectory in `skills/` is a portable agent skill bundle containing a `SKILL.md` file.

Use this directory for skill content only. Other artifact types should live in their own top-level directories.
